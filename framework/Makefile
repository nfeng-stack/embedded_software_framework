# Framework layer Makefile
# Build framework source files

obj-y += src/
obj-y += ports/$(FRAMEWORK_PORT)/

# Framework-specific C flags
# EXTRA_CFLAGS += -DFRAMEWORK_ENABLED=1

# Include local configuration if exists
-include local.mk