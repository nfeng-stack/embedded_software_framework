# HAL STM32F4 platform driver Makefile
# Define object files to build into built-in.o

obj-y :=

# Platform-specific C flags (if any)
# EXTRA_CFLAGS += -DPLATFORM_STM32F4

# Include local configuration if exists
-include local.mk