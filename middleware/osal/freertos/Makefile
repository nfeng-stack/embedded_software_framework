# OSAL FreeRTOS port Makefile
# Define object files to build into built-in.o

obj-y :=

# RTOS-specific C flags (if any)
# EXTRA_CFLAGS += -DUSE_FREERTOS

# Include local configuration if exists
-include local.mk