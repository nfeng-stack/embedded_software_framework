# STM32H5xx Templates Makefile
# Build system initialization files

obj-y += system_stm32h5xx.o

# Template specific flags
# EXTRA_CFLAGS += -DUSE_SYSTEM_INIT

# Include local configuration if exists
-include local.mk