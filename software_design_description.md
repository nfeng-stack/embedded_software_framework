# 系统软件设计

本文所述嵌入式跌倒检测系统基于 STM32H563（Cortex-M33 内核，主频 250 MHz）平台，搭载 RT-Thread 实时操作系统，采用三层软件架构（硬件抽象层 HAL、操作系统抽象层 OSAL、应用层 Application）进行模块化设计。系统软件总计约 119,000 行代码，其中自主编写的核心业务逻辑约 7,000 行，所适配的开源项目包括 LibDriver、FatFs、cJSON、EasyLogger、TinyUSB、RT-Thread，以及由 ST X-CUBE-AI 工具链自动生成的神经网络推理代码。以下对系统各主要软件功能模块进行逐一介绍。

---

## 一、MPU6050 六轴传感器数据采集模块

### 功能描述

该模块负责驱动板载 MPU6050 六轴惯性测量单元（IMU），完成加速度和角速度数据的实时采集，并基于传感器硬件中断实现跌倒触发信号的初步检测。模块运行方式为事件驱动设计，无需 CPU 轮询，仅在传感器产生硬件中断时唤醒系统。

### 实现能力

MPU6050 传感器配置自由落体检测阈值为 700 mg、持续时间为 1 ms。当人体发生跌倒时，合加速度骤降至接近零，传感器检测到自由落体条件满足后，INT 引脚产生硬件中断信号。该中断通过 STM32 GPIO（引脚 41）触发 MCU 唤醒，进入中断服务程序。中断回调函数释放信号量 `mpu_sem`，唤醒 MPU6050 数据采集任务。

采集任务首先禁用自由落体中断、使能数据就绪中断，随后以 200 Hz 采样率连续采集 200 组六轴原始数据（三轴加速度 + 三轴陀螺仪），耗时约 1 秒。每组数据量化归一化至 0–255 范围（加速度饱和区间 ±16g，陀螺仪饱和区间 ±2000°/s），供后续 AI 神经网络推理使用。量化完成后释放信号量 `ai_sem` 通知 AI 推理任务。采集任务随后恢复自由落体中断监测模式，系统重新进入低功耗等待状态。

### 技术优势

| 特性 | 说明 |
|------|------|
| **中断驱动，零轮询功耗** | 传感器硬件级阈值判决，MCU 在无事件时处于空闲状态 |
| **硬件级触发延迟 <1 μs** | 自由落体检测由 MPU6050 内部 DSP 完成，无需软件参与 |
| **200 组连续数据** | 完整捕获跌倒前后的运动特征，为 AI 推理提供充足时序信息 |

### 代码量

| 类别 | 代码行数 | 说明 |
|------|---------|------|
| 自写代码 | ~440 行 | 传感器任务封装 `mpu6050_wrap.c`（中断驱动逻辑、量化归一化、任务调度）、数据记录器 `mpu6050_data_logger.c` |
| 适配代码 | ~15,200 行 | 移植自 [LibDriver](https://github.com/ShifengLi/libdriver) 社区开源项目（Shifeng Li 开发，MIT 协议）的全功能 MPU6050 驱动，含 DMP 固件加载、FIFO 管理、寄存器级自检、I2C 接口模板适配等 |

> 模块总代码量约 **16,000 行**。

---

## 二、AI 神经网络跌倒推理模块

### 功能描述

该模块基于 ST X-CUBE-AI 深度学习运行时框架，在嵌入式端侧部署并运行卷积神经网络（CNN）模型，对 MPU6050 采集的运动数据进行分类推理，判断是否发生跌倒事件。整个推理过程在 MCU 本地完成，无需上传云端，无隐私数据外泄风险。

### 实现能力

所部署的神经网络模型名为 `fallnet_stm32`，采用五层经典 CNN 架构：

1. **Conv2D 层 1**：输入 3 通道，输出 32 通道，5×5 卷积核，步长 1，ReLU 激活，2×2 最大池化 → 输出尺寸 32×10×10
2. **Conv2D 层 2**：输入 32 通道，输出 64 通道，5×5 卷积核，步长 1，ReLU 激活，2×2 最大池化 → 输出尺寸 64×5×5
3. **Reshape + 全连接 FC1**：1,600 维输入 → 512 维输出，ReLU 激活
4. **FC2 输出层**：512 维 → 2 维（两个类别 logits）
5. **Softmax 后处理**：将 logits 转换为概率分布

模型总计算量约 700 万次乘加运算（MACs），在 Cortex-M33 内核（含硬件 FPU）上的单次推理耗时约 50 ms。权重参数约 1 MB，推理所需激活内存约 27 KB。

为有效抑制误报，判决环节采用**双重阈值策略**——当且仅当 Softmax 输出的跌倒概率大于 0.5 且对应 logit 值大于等于 5 时，才认定为跌倒事件。若判决为非跌倒，系统恢复自由落体监测；若判决为跌倒，则触发声光报警和后续紧急处理流程。

### 技术优势

| 特性 | 说明 |
|------|------|
| **全离线推理** | 不依赖网络连接和云服务，适用于户外和信号盲区 |
| **无隐私风险** | 原始传感器数据不出设备，不涉及隐私合规问题 |
| **双重阈值防误报** | 结合概率阈值和 logit 绝对值双重过滤，有效抑制非跌倒状态的误触发 |
| **低推理延迟** | <50 ms，满足实时性要求 |

### 代码量

| 类别 | 代码行数 | 说明 |
|------|---------|------|
| 自写代码 | ~78 行 | AI 任务调度 `ai_task.c`（信号量等待、结果处理、报警触发） |
| 自动生成代码 | ~34,350 行 | 由 ST [X-CUBE-AI](https://www.st.com/en/embedded-software/x-cube-ai.html) 深度学习工具链从训练后的 TensorFlow / Keras 模型自动生成，包含网络拓扑定义、权重数据（约 32,500 行）、推理管线代码 |
| 外部运行时库 | 预编译 .a 文件 | `NetworkRuntime1020_CM33_GCC.a`，Cortex-M33 专用神经网络推理引擎 |

> 模块总代码量约 **34,400 行**（不含预编译运行时库）。

---

## 三、4G 通信与定位模块

### 功能描述

该模块通过 UART2 串口驱动板载 4G/LTE 蜂窝通信模组，实现基站网络注册、GPS 卫星定位、HTTPS 云服务调用、SMS 短信发送等功能，构成跌倒事件发生后的远程报警通道。

### 实现能力

本模块包含以下几个方面的核心能力：

**（1）AT 指令引擎**：实现了完整的 AT 指令收发引擎，支持超时重试、多结束标记匹配（OK、ERROR、+CME ERROR、CONNECT、+QHTTPGET 等）、缓冲区溢出保护等容错机制。核心函数 `at_rsp_wait()` 以逐字符读取方式解析 UART 响应流。

**（2）网络初始化流程**：上电后自动执行完整的蜂窝网络注册流程——AT 通信测试 → SIM 卡状态检查（+CPIN?）→ 信号强度查询（+CSQ）→ 网络注册状态确认（+CGREG?）→ GPS 使能（+QGPS=1）→ PS 域网关附着（+CGATT?）→ PDP 上下文配置（+QICSGP，中国联通 UNINET APN）→ PDP 激活（+QIACT=1）。

**（3）GPS 定位与 NMEA 解析**：发送 `AT+QGPSGNMEA="RMC"` 指令获取推荐最小定位信息（RMC）报文，通过手写 NMEA 解析器解析 `$GNRMC` / `$GPRMC` 语句，将 ddmm.mmmmm 格式转换为十进制度经纬度，并校验 GPS 定位有效性标志。

**（4）逆地理编码**：将经纬度坐标通过 HTTPS GET 请求发送至高德开放平台逆地理编码 API，获取对应的街道级地址描述（formatted_address），供报警消息使用。

**（5）微信报警推送**：通过巴法云（Bemfa）IoT 平台的微信消息推送接口，以 HTTPS GET 方式将组装好的报警信息（地址、坐标、时间戳）推送至监护人微信客户端。

**（6）SMS 备用通道**：当 4G 数据连接不可用时，自动切换为 SMS 短信方式发送报警。

### 技术优势

| 特性 | 说明 |
|------|------|
| **100% 自主编写** | AT 指令引擎、NMEA 解析器、HTTP 客户端、URL 编码器、字符串工具函数均为自实现，无外部库依赖 |
| **多通道报警冗余** | 微信推送（主通道）+ SMS 短信（备用通道）双重保障 |
| **地址级定位** | 经高德逆地理编码获得街道级地址，而非仅有经纬度坐标 |
| **容错机制完备** | 超时检测、重试、缓冲区溢出保护 |

### 代码量

| 类别 | 代码行数 | 说明 |
|------|---------|------|
| 自写代码 | ~1,560 行 | `at_command.c`（约 1,250 行，AT 引擎 + HTTPS + GPS + SMS + 字符串库） + `at_task.c`（约 290 行，任务调度 + 高德/巴法云 API 调用逻辑） |

> 模块总代码量约 **1,560 行**，**全部为自主编写，无第三方依赖**。

---

## 四、传感器数据持久化模块

### 功能描述

该模块负责将每次跌倒事件触发时采集的 200 组原始六轴传感器数据实时写入文件系统，以供事后回放分析和模型迭代优化。存储介质为板载 W25Qxx NOR Flash（16 MB），文件系统采用 FATFS。

### 实现能力

模块以 `mpu6050_data_logger_write_batch()` 函数为核心，在 AI 推理启动前完成数据写入。每批次数据包含系统时间戳和 200 组加速度 + 陀螺仪的原始数值，写入路径为 `/data/mpu6050_data.txt`。

模块内置以下关键机制：

- **文件轮转**：当数据文件大小超过 1 MB 时，自动将当前文件重命名为 `mpu6050_data_<tick>.txt`，并创建新文件继续写入，避免单文件过大导致的文件系统性能退化。
- **USB 冲突规避**：通过 `usb_state_is_connected()` 查询 USB MSC 大容量存储设备是否已被宿主机挂载。若已挂载，自动跳过本次写入操作，防止宿主机与 MCU 同时对文件系统进行写操作而产生数据损坏。
- **故障自停**：连续 5 次写入失败后，模块自动进入错误状态并停止后续写入，避免持续无效操作消耗系统资源。

### 技术优势

| 特性 | 说明 |
|------|------|
| **断电不丢失** | NOR Flash 非易失存储，数据掉电保持 |
| **文件轮转机制** | 自动归档历史数据，防止单文件过大 |
| **USB 冲突规避** | 配合 U 盘功能保证文件系统一致性 |

### 代码量

| 类别 | 代码行数 | 说明 |
|------|---------|------|
| 自写代码 | ~440 行 | `mpu6050_data_logger.c` 完整实现，无第三方依赖 |

> 模块总代码量约 **440 行**，**全部为自主编写**。

---

## 五、文件系统与 U 盘存储模块

### 功能描述

该模块在板载 W25Qxx NOR Flash（16 MB）上运行 FATFS 文件系统，并通过 USB MSC（Mass Storage Class）协议将 Flash 存储空间映射为宿主机可识别的大容量存储设备（即"U 盘"）。用户使用 USB 数据线连接设备到 PC 后，无需安装任何驱动或软件即可在操作系统中看到一个标准可移动磁盘，直接打开 txt 文件查看传感器日志。

### 实现能力

模块由三个核心层次构成：

**（1）Flash 底层驱动层**：适配 LibDriver 社区的全功能 W25Qxx SPI/QSPI NOR Flash 驱动，支持标准 SPI 和 Quad SPI 两种通信模式，提供扇区擦除（4 KB）、块擦除（32 KB / 64 KB）、全片擦除三级擦除粒度，以及页编程（256 字节）、多种读取模式（标准/快速/双线/四线）、JEDEC ID 读取、掉电管理等全系列操作。

**（2）文件系统层**：移植 FatFs R0.16（ChanN 开发）作为文件系统核心引擎，提供 FAT12/16/32 格式支持。自建的 FatFs 服务封装层 `fatfs_service.c`（约 1,180 行）向上层提供符合本系统调用习惯的文件操作 API（open、close、read、write、mkdir、rename、stat、exists、sync、format），并封装了错误码返回和句柄管理。

**（3）USB MSC 协议层**：移植 TinyUSB（Ha Thach 开发，MIT 协议）USB 协议栈的大容量存储类驱动，实现 SCSI 命令响应（INQUIRY、READ_CAPACITY_10、READ_10、WRITE_10、START_STOP、PREVENT_ALLOW_MEDIUM_REMOVAL、SYNCHRONIZE_CACHE）。MSC 读写回调函数桥接到 FatFS 的 `disk_read()` / `disk_write()` 接口。USB 设备状态（挂载/卸载/挂起）以 volatile 全局标志实时通知其它模块。

### 技术优势

| 特性 | 说明 |
|------|------|
| **PC 免驱免软件** | 即插即用，FAT 格式操作系统原生兼容 |
| **QSPI 高速读写** | 四线 SPI 模式提供优于标准 SPI 的吞吐性能 |
| **复合设备共存** | U 盘与虚拟串口共享一个 USB 物理接口 |

### 代码量

| 类别 | 代码行数 | 说明 |
|------|---------|------|
| 自写代码 | ~2,350 行 | FATFS 服务封装 `fatfs_service.c`、磁盘 I/O 层 `diskio.c`、USB 协议管理 `usb_protocol.c`、USB 状态管理 `usb_state.c` |
| 适配代码 | ~32,740 行 | FatFs 核心（ChanN）~19,650 行 + LibDriver W25Qxx 驱动（Shifeng Li，MIT）~11,630 行 + TinyUSB MSC（Ha Thach，MIT）~1,460 行 |

> 模块总代码量约 **34,500 行**（不含 TinyUSB 核心协议栈）。

---

## 六、USB 虚拟串口模块

### 功能描述

该模块基于 TinyUSB CDC（Communications Device Class）协议实现 USB 虚拟串口功能，与 MSC U 盘功能在 USB 复合设备框架下共存于同一 USB 物理接口。虚拟串口主要用于系统调试——将 EasyLogger 日志输出、RT-Thread 内核控制台输出和自定义调试信息实时发送至宿主机端的串口终端软件。

### 实现能力

TinyUSB 复合设备配置为两个接口——接口 0/1 分配给 CDC（虚拟串口），接口 2 分配给 MSC（U 盘）。上电或 USB 插入后，`usb_protocol_init()` 初始化 TinyUSB 设备栈，创建 USB 事件处理任务（2 KB 栈空间，优先级 8），持续调用 `tud_task()` 处理 USB 枚举、数据传输和设备状态管理。

模块提供 `usb_state_is_connected()` 查询接口供传感器数据持久化模块在写入 Flash 前查询 USB MSC 挂载状态，从而规避宿主机与设备同时对文件系统进行写操作时的数据冲突。

### 技术优势

| 特性 | 说明 |
|------|------|
| **无需额外硬件** | 与 U 盘共用一根 USB 数据线完成调试 |
| **复合设备** | CDC + MSC 共存于同一 USB 接口 |

### 代码量

| 类别 | 代码行数 | 说明 |
|------|---------|------|
| 自写代码 | ~470 行 | USB 协议管理 `usb_protocol.c`、状态管理 `usb_state.c`、任务调度 |

> 模块自写代码量约 **470 行**（底层 TinyUSB CDC 协议栈计入框架 SDK）。

---

## 七、JSON 解析模块

### 功能描述

该模块基于 cJSON 开源库，为 4G 通信与定位模块提供云端 API 返回数据的 JSON 格式解析能力。在本系统中，主要解析高德逆地理编码 API 的返回数据（提取 formatted_address、经纬度、返回状态码），以及巴法云微信推送 API 的返回结果（确认推送是否成功）。

### 实现能力

cJSON 是一个仅有 cJSON.c 和 cJSON.h 两个源文件的超轻量级 JSON 解析器，无需任何外部依赖，编译产物约 80 KB。为适配本系统的 RT-Thread 堆内存管理机制，编写了 cjson_porting.c 适配层，其 `cJSON_Porting_Init()` 函数将 cJSON 库默认的 `malloc()` / `free()` 函数指针替换为 RT-Thread 内核的 `osal_malloc()` / `osal_free()`，确保 JSON 解析过程中的动态内存分配发生在 RT-Thread 堆（400 KB）而非 Newlib 堆（16 KB）中，避免解析大体积 JSON 报文时发生内存不足。

### 技术优势

| 特性 | 说明 |
|------|------|
| **轻量级** | ~80 KB 编译产物，无外部依赖，适合嵌入式平台 |
| **堆内存适配** | 适配 RT-Thread 堆管理，与系统内存布局协调 |

### 代码量

| 类别 | 代码行数 | 说明 |
|------|---------|------|
| 自写代码 | ~69 行 | RT-Thread 堆内存接口适配层 `cjson_porting.c` |
| 适配代码 | ~3,510 行 | [cJSON](https://github.com/DaveGamble/cJSON) 开源库（Dave Gamble 等贡献者，MIT 协议） |

> 模块总代码量约 **3,580 行**。

---

## 八、日志服务模块

### 功能描述

该模块基于 EasyLogger 开源日志库，为系统各软件模块提供多级别、带标签过滤的调试日志输出功能。日志输出目标为 UART1 调试串口，可通过串口终端软件实时查看。

### 实现能力

EasyLogger 提供五个日志级别（Verbose、Debug、Info、Warning、Error），各模块通过定义 `LOG_TAG` 宏（如 `"mpu6050"`、`"at_cmd"`、`"ai_pre"`、`"at_task"`）携带模块标识，便于按模块过滤和区分日志来源。日志输出采用异步环形缓冲区设计——调用 `log_i()` 等 API 时，日志内容被写入环形缓冲区后立即返回，由后台任务负责异步输出，不会阻塞业务线程的实时执行。

为适配本系统硬件平台，编写了 UART1 串口输出端口适配层 `elog_port.c`，将 EasyLogger 的底层字符输出函数重定向至 HAL 层的 `hal_uart1_write()`。

### 技术优势

| 特性 | 说明 |
|------|------|
| **异步零阻塞** | 环形缓冲区 + 后台输出，不影响业务线程实时性 |
| **模块级过滤** | 支持按 LOG_TAG 启用或关闭特定模块的日志输出 |
| **五级分级** | Verbose / Debug / Info / Warn / Error 五级可控 |

### 代码量

| 类别 | 代码行数 | 说明 |
|------|---------|------|
| 自写代码 | ~175 行 | UART1 输出端口适配层 `elog_port.c` |
| 适配代码 | ~1,915 行 | [EasyLogger](https://github.com/armink/EasyLogger) 开源库（Armink 开发，MIT 协议），含核心引擎、异步模块、缓冲区和工具函数 |

> 模块总代码量约 **2,090 行**。

---

## 九、RT-Thread 实时操作系统适配

### 功能描述

本系统以 RT-Thread 为底层实时操作系统（RTOS），通过自建的 OSAL（操作系统抽象层）实现应用层与 RTOS 内核的解耦，使应用代码无需直接调用 RT-Thread 内核 API，从而实现 RTOS 的可移植性。

### 实现能力

**（1）两层 OSAL 抽象架构**

OSAL 由两个层次构成——Common API 层和 RT-Thread 适配层。

Common API 层（约 551 行）定义了标准的操作系统抽象接口，涵盖 11 个 API 类别：任务管理（`osal_task_create` / `delete` / `delay` / `suspend` / `resume` 等 13 个函数）、信号量（8 个函数）、互斥量（8 个函数）、事件标志组（7 个函数）、邮箱（9 个函数）、消息队列（9 个函数）、软件定时器（7 个函数）、内存池（6 个函数）、动态内存管理（`osal_malloc` / `free` / `realloc` / `calloc` / `heap_init`）、系统服务（`osal_tick_get` / `enter_critical` / `exit_critical` 等）和对象管理。每一类 API 均以纯转发方式实现，不包含任何业务逻辑。

RT-Thread 适配层（约 910 行）将 Common API 层的 58 个函数一对一映射到 RT-Thread 内核 API（如 `osal_task_create()` 映射为 `rt_thread_create()`），包括类型别名（`_osal_task_t` 定义为 `rt_thread_t`）、错误码映射（`_OSAL_EOK` 映射为 `RT_EOK` 等 10 种错误码）和接口函数封装，每个适配函数平均仅约 15 行代码。

**（2）双堆内存布局**

系统在 RAM 中划分了两个独立的堆区域——Newlib 堆（16 KB）用于 C 标准库函数（如 `printf`、文件 I/O 缓冲）的动态内存需求，RT-Thread 堆（400 KB）专门用于 RTOS 内核对象（线程栈、信号量、互斥量等）以及通过 `osal_malloc()` 进行的应用层动态内存分配。两个堆之间设有 4 KB 的隔离缓冲区，防止堆交叉污染。

**（3）栈溢出保护**

在 MSP 主栈底部设置 4 KB 的保护区域，启动时填充 `0xDEADBEEF` 哨兵模式，运行时可通过检查该区域数据的完整性来判断是否发生栈溢出。

**（4）SysTick 双回调链**

STM32 SysTick 定时器中断服务程序中维护一个双槽位回调函数数组，槽位 0 挂载 HAL 层时钟驱动，槽位 1 挂载 RT-Thread 内核的 `rt_tick_increase()` 函数。回调注册采用延迟绑定机制——OSAL 层首先通过 `rt_register_callback()` 注册回调函数指针，待 RT-Thread 内核在 `rt_hw_board_init()` 中初始化堆后统一激活，确保内核滴答时钟在调度器启动前就已正确配置。

**（5）RT-Thread 内核配置**

系统配置 RT-Thread 内核参数如下：优先级最大 32 级，系统滴答周期 1 ms，主线程栈空间 3 KB。启用的内核组件包括动态堆（小内存分配器）、信号量、互斥量、事件标志组、邮箱、消息队列、内存池、控制台（输出重定向至 EasyLogger）和组件自动初始化。

**（6）RTOS 可替换性**

OSAL 同时预留了 FreeRTOS 适配器的接口定义文件 `osal_port.h`（约 154 行），声明了与 RT-Thread 适配层完全一致的 58 个底层函数签名。仅需按相同模式编写 FreeRTOS 对这些函数的实现代码（约 60 个适配函数），即可将系统底层 RTOS 从 RT-Thread 切换为 FreeRTOS，上层应用代码无需任何修改。

### 技术优势

| 特性 | 说明 |
|------|------|
| **RTOS 可替换** | OSAL 抽象层解耦，更换 RTOS 仅需编写约 60 个适配函数 |
| **双堆隔离** | Newlib 堆与 RT-Thread 堆物理隔离，4 KB 隔离带防止交叉污染 |
| **栈溢出两层防护** | MPU 硬件保护 + 0xDEADBEEF 哨兵软件检测 |
| **薄适配层** | 适配层仅约 1,460 行（占 RT-Thread 内核 26,000 行的 5.6%），抽象开销极低 |

### 代码量

| 类别 | 代码行数 | 说明 |
|------|---------|------|
| 自写代码 | ~1,660 行 | Common API 层约 551 行 + RT-Thread 适配层约 910 行 + Board 支持包约 200 行 |
| 适配代码 | ~26,000 行 | [RT-Thread](https://github.com/RT-Thread/rt-thread) v4.x 内核（kernel 核心 + Cortex-M33 CPU 移植 + 头文件），Apache-2.0 协议 |

> RTOS 适配总计约 **27,660 行代码**。

---

## 总体代码统计

| 模块 | 总代码量 | 自写代码 | 适配/生成代码 | 适配来源 |
|------|---------|---------|-------------|---------|
| MPU6050 数据采集 | ~16,000 行 | ~440 行 | ~15,200 行 | LibDriver（MIT） |
| AI 神经网络推理 | ~34,400 行 | ~78 行 | ~34,350 行 | ST X-CUBE-AI（自动生成） |
| 4G 通信与定位 | ~1,560 行 | ~1,560 行 | 0 | 无（自主编写） |
| 传感器数据持久化 | ~440 行 | ~440 行 | 0 | 无（自主编写） |
| 文件系统与 U 盘 | ~34,500 行 | ~2,350 行 | ~32,740 行 | FatFs + LibDriver + TinyUSB（MIT） |
| USB 虚拟串口 | ~470 行 | ~470 行 | SDK 级协议栈 | TinyUSB CDC（MIT） |
| JSON 解析 | ~3,580 行 | ~69 行 | ~3,510 行 | cJSON（MIT） |
| 日志服务 | ~2,090 行 | ~175 行 | ~1,915 行 | EasyLogger（MIT） |
| RT-Thread RTOS 适配 | ~27,660 行 | ~1,660 行 | ~26,000 行 | RT-Thread（Apache-2.0） |
| **合计** | **~119,000 行** | **~7,240 行** | **~115,700 行** | |

---

*此文档用于毕业论文章节 "系统软件设计" 的技术说明。*
